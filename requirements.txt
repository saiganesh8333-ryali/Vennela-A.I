# Web framework (stable, Python 3.11 compatible)
fastapi==0.115.0
uvicorn==0.30.6
gunicorn==23.0.0

# Firebase with locked stable versions (Python 3.11 compatible)
firebase-admin==6.5.0
google-cloud-firestore==2.16.0

# Environment config
python-dotenv==1.0.1

# HTTP utilities
requests==2.32.3

# LLM API (stable version, Python 3.11 compatible)
google-generativeai==0.5.4

# NOTE: Heavy ML libraries replaced with lightweight Python modules:
# - sentence-transformers (200MB) -> lightweight_embeddings.py
# - transformers (500MB) -> lightweight_nlp.py
# - torch (500MB) -> removed (not needed for chat)
# - scikit-learn (100MB) -> lightweight_ml.py
# - numpy (removed - not used in simple chat endpoint)
# This reduces deployment size from 1.3GB to ~25MB
#
# Python 3.11 used for compatibility. Avoid 3.14 (no prebuilt numpy wheels).
# All versions locked to prevent dependency resolver conflicts.
