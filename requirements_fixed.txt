# Web framework
fastapi==0.116.1
uvicorn[standard]==0.35.0
gunicorn==23.0.0
pydantic==2.5.3

# Firebase
firebase-admin
google-cloud-firestore
google-api-core

# Core data processing (lightweight alternatives to torch/transformers/sklearn)
numpy

# Environment config
python-dotenv

# LLM API
google-generativeai==0.7.2
protobuf==4.25.3
grpcio==1.60.0

# NOTE: Heavy ML libraries replaced with lightweight Python modules:
# - sentence-transformers (200MB) -> lightweight_embeddings.py
# - transformers (500MB) -> lightweight_nlp.py
# - torch (500MB) -> numpy
# - scikit-learn (100MB) -> lightweight_ml.py
# This reduces deployment size from 1.3GB to ~30MB
